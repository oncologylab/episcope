% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_step1_grn_preprocess.R
\name{filter_by_min_bound}
\alias{filter_by_min_bound}
\title{Filter GRN peaks by minimum number of bound samples}
\usage{
filter_by_min_bound(
  grn_set,
  min_bound = 1L,
  samples = NULL,
  na_as_unbound = TRUE,
  verbose = TRUE,
  check_align = FALSE
)
}
\arguments{
\item{grn_set}{List containing \code{fp_score}, \code{fp_bound},
and \code{fp_annotation}.}

\item{min_bound}{Integer threshold (default 1).}

\item{samples}{Optional character vector of sample IDs to consider;
default uses all sample columns in \code{fp_bound}.}

\item{na_as_unbound}{Logical; treat NA as unbound (0) if TRUE, otherwise
drop rows with any NA among the selected samples.}

\item{verbose}{Logical; print progress messages.}

\item{check_align}{Logical; if TRUE, assert alignment before filtering.}
}
\value{
A filtered \code{grn_set} list with aligned fp tables.
}
\description{
Keeps rows where the count of bound samples in \code{fp_bound} meets
\code{min_bound}, and applies the same peak filter to the aligned
fp_score/fp_bound/fp_annotation tables.
}
