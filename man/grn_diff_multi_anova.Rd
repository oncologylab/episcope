% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_grn_diff_multi.R
\name{grn_diff_multi_anova}
\alias{grn_diff_multi_anova}
\title{Multi-condition ANOVA-like test on TF–gene link scores (limma F-test)}
\usage{
grn_diff_multi_anova(
  inputs,
  sample_info,
  condition_col = "condition",
  value_col = "link_score",
  strict = TRUE,
  dedupe = TRUE,
  dedupe_method = c("max_link_score", "first"),
  verbose = TRUE
)
}
\arguments{
\item{inputs}{Character vector of CSVs or list of data.frames/tibbles,
one per sample/condition.}

\item{sample_info}{Data.frame with one row per input, in the same order.
Must contain a column specified by `condition_col` (default `"condition"`),
which will be treated as a factor.}

\item{condition_col}{Column in `sample_info` defining the groups (default
`"condition"`).}

\item{value_col}{Column in the link tables to model (default `"link_score"`).}

\item{strict, }{dedupe, dedupe_method Passed to `.load_multi_links_matrix()`.}

\item{verbose}{Logical.}
}
\value{
A tibble with:
  - `tf`, `gene_key`, `peak_id`.
  - `F_stat`, `p_value`, `fdr`.
  - One column per design coefficient: `coef_<design_colname>`.
}
\description{
This function performs a formal multi-group test per edge using limma,
analogous to an ANOVA F-test across all conditions at once (no pairwise
comparisons). Each input is one "sample" (e.g., a condition-averaged link
table, or one replicate).
}
