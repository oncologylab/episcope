% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_grn_lda.R
\name{annotate_links_with_lda_topic_bulk}
\alias{annotate_links_with_lda_topic_bulk}
\title{Bulk/parallel LDA annotation over many filtered CSVs}
\usage{
annotate_links_with_lda_topic_bulk(
  filtered_csvs,
  K = 20,
  which = c("abs", "gain", "loss"),
  min_df = 2L,
  method = c("VEM", "Gibbs"),
  seed = 1L,
  gamma_cutoff = 0.2,
  multi_sep = ";",
  save_csv = TRUE,
  parallel = TRUE,
  plan = "multisession",
  workers = NULL,
  write_manifest = TRUE,
  manifest_file = NULL,
  verbose = TRUE
)
}
\arguments{
\item{filtered_csvs}{Character vector of filtered CSV paths
(e.g., \code{bulk$filtered_paths} from filter step).}

\item{K}{Integer, number of topics (default 20).}

\item{which}{One of 'abs' (default), 'gain', 'loss' to form weights from delta_link_score.}

\item{min_df}{Integer; minimum TF-document frequency for a gene term (default 2).}

\item{method}{'VEM' (default) or 'Gibbs' (passed to topicmodels::LDA).}

\item{seed}{Integer RNG seed (default 1).}

\item{gamma_cutoff}{Numeric; keep all topics with gamma >= cutoff (default 0.2).}

\item{multi_sep}{String to separate multiple topic IDs (default ';').}

\item{save_csv}{Logical; if TRUE and `x` is a CSV path, write alongside input
with suffix \code{_lda_K{K}.csv} (default TRUE).}

\item{parallel}{Logical; run via future.apply. Default TRUE.}

\item{plan}{Future plan; string or function. Default "multisession".}

\item{workers}{Integer; default parallel::detectCores()-1.}

\item{write_manifest}{Logical; write a manifest CSV mapping source->annotated (default TRUE).}

\item{manifest_file}{Optional path for manifest; if NULL, writes next to the first input as
"delta_links_lda_manifest.csv".}

\item{verbose}{Logical; default TRUE.}
}
\value{
A list with:
  \itemize{
    \item results: named list of annotated tibbles
    \item assigned_paths: character vector of output CSVs
    \item assigned_dirs: unique directories
    \item manifest: tibble (source_filtered, annotated_csv, exists)
    \item manifest_path: path where manifest was written (or NULL)
  }
}
\description{
Bulk/parallel LDA annotation over many filtered CSVs
}
