% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_step3_topic_models.R
\name{add_condition_tf_docs}
\alias{add_condition_tf_docs}
\title{Add condition-level topic-model document IDs to links}
\usage{
add_condition_tf_docs(
  edges,
  tf_cluster_map = NULL,
  doc_mode = c("tf_cluster", "tf")
)
}
\arguments{
\item{edges}{Link table.}

\item{tf_cluster_map}{Named vector mapping TF names to clusters. Required
for `doc_mode = "tf_cluster"`.}

\item{doc_mode}{Document mode, either `"tf_cluster"` or `"tf"`.}
}
\value{
A data.table with condition-specific rows and `doc_id`.
}
\description{
Expands per-comparison links into condition-specific rows for both case and
control conditions. For `doc_mode = "tf_cluster"`, the document key is
`condition_label::tf_cluster`. For `doc_mode = "tf"`, the document key is
`condition_label::tf`.
}
\details{
This helper does not itself filter target genes, peaks, or TF documents by
thresholds. It records condition-specific TF expression, target-gene
expression, and footprint score so `build_doc_term_condition_union()` can
apply threshold rules consistently during term construction.

Required input columns are `comparison_id`, `case_id`, `ctrl_id`, `tf`,
`gene_key`, `peak_id`, `fp_score_case`, `fp_score_ctrl`,
`gene_expr_case`, and `gene_expr_ctrl`. TF expression columns
`tf_expr_case` and `tf_expr_ctrl` are optional for legacy cluster-level
workflows but required when condition-level TF thresholds are applied.
}
\keyword{internal}
