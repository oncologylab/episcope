% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_step3_topic_benchmark.R
\name{run_module3_topic_benchmark}
\alias{run_module3_topic_benchmark}
\title{Run a Module 3 topic benchmark and review report}
\usage{
run_module3_topic_benchmark(
  filtered_dir,
  multiomic_data = NULL,
  comparisons,
  output_dir,
  methods = "condition_aggr_weight_lda",
  k_grid = 10L,
  replicate_documents = FALSE,
  reuse_if_exists = TRUE,
  local_threads = NULL,
  run_training = TRUE,
  run_extraction = TRUE,
  run_reports = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{filtered_dir}{Directory containing Module 3 filtered differential-link
files.}

\item{multiomic_data}{Optional CraftGRN multiomic object. Reserved for future
package-level replicate document expansion.}

\item{comparisons}{Comparison or condition grouping table, or a CSV path.
For condition separation scoring, use columns `condition_label` and
`condition_group`.}

\item{output_dir}{Topic benchmark output directory.}

\item{methods}{Character vector of benchmark method IDs, `"default"`, or
`"all"`.}

\item{k_grid}{Integer topic numbers.}

\item{replicate_documents}{Whether documents are replicate resolved.}

\item{reuse_if_exists}{Reuse existing model outputs where possible.}

\item{local_threads}{Optional thread count for model training.}

\item{run_training}{Train topic models before reporting.}

\item{run_extraction}{Run topic extraction before reporting.}

\item{run_reports}{Build score tables and review reports.}

\item{verbose}{Emit concise progress messages.}
}
\value{
A list with method plan, discovered model rows, score results, and
  review report paths.
}
\description{
Runs or reviews Module 3 topic models using the package-native benchmark
layout. The function can train/extract models, or it can score and report
existing theta/phi and topic-link outputs when `run_training = FALSE` and
`run_extraction = FALSE`.
}
