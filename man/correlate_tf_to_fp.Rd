% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_step1_tf_correlation.R
\name{correlate_tf_to_fp}
\alias{correlate_tf_to_fp}
\title{Correlate TF expression to footprint scores for Module 1 TFBS prediction}
\usage{
correlate_tf_to_fp(
  omics_data = NULL,
  grn_set = NULL,
  config = NULL,
  genome = NULL,
  gene_symbol_col = "HGNC",
  fp_aligned = NULL,
  do_preprocess = FALSE,
  do_motif_clustering = FALSE,
  fp_root_dir = NULL,
  fp_cache_dir = NULL,
  fp_cache_tag = NULL,
  mode = c("canonical", "all"),
  label_col,
  out_dir = NULL,
  db = NULL,
  r_thr = 0.3,
  p_thr = 0.05,
  tf_subset = NULL,
  cores_pearson = NULL,
  cores_spearman = NULL,
  chunk_size = 5000L,
  all_mode_tf_chunk_size = 5L,
  canonical_mode_tf_chunk_size = 50L,
  min_non_na = 5L,
  qc = TRUE,
  write_bed = FALSE,
  overwrite_tf_overview = FALSE,
  write_direct_bound = TRUE,
  direct_bound_conditions = NULL,
  write_outputs = TRUE,
  use_cache = TRUE,
  cache_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{omics_data}{Optional prepared Module 1 multi-omic object.}

\item{grn_set}{Deprecated synonym for `omics_data`.}

\item{config}{Optional YAML config path used when `omics_data` is not
supplied.}

\item{genome}{Optional genome override.}

\item{gene_symbol_col}{Gene-symbol column in the RNA table.}

\item{fp_aligned}{Optional aligned footprint object used when rebuilding
`omics_data` inline.}

\item{do_preprocess}{Logical; if `TRUE`, rebuild Step 1 preprocessing before
correlation.}

\item{do_motif_clustering}{Logical; if `TRUE`, allow motif clustering during
inline preprocessing.}

\item{fp_root_dir, fp_cache_dir, fp_cache_tag}{Optional footprint source and
cache settings for inline preprocessing.}

\item{mode}{Correlation mode. The rebuilt Step 1 path uses `"all"` as the
default and primary mode.}

\item{label_col}{Metadata column used to aggregate matched conditions.}

\item{out_dir}{Output directory for Step 1 TFBS files.}

\item{db}{Motif database label used in output file names.}

\item{r_thr}{Correlation threshold applied to `max(Pearson_r, Spearman_r)`.}

\item{p_thr}{Retained for compatibility but not used in the final rebuilt
TFBS call rule.}

\item{tf_subset}{Optional TF subset for quick downstream correlation checks.}

\item{cores_pearson, cores_spearman}{Optional worker counts for the two
correlation methods.}

\item{chunk_size}{Correlation chunk size.}

\item{all_mode_tf_chunk_size, canonical_mode_tf_chunk_size}{Retained for
compatibility with the legacy interface.}

\item{min_non_na}{Minimum number of shared finite values required to compute
a correlation.}

\item{qc}{Logical; if `TRUE`, write correlation QC and overview outputs.}

\item{write_bed}{Logical; retained for compatibility. The rebuilt Step 1 path
does not yet write BED outputs.}

\item{overwrite_tf_overview}{Logical; if TRUE, rewrite existing per-TF
overview files. By default existing overview files are kept and skipped.}

\item{write_direct_bound}{Logical; if `TRUE`, write condition-specific
direct-bound BED files as part of the standard Module 1 workflow.}

\item{direct_bound_conditions}{Optional condition subset for direct-bound BED
writing. Defaults to all conditions in `fp_bound_condition`.}

\item{write_outputs}{Logical; if `TRUE`, save filtered score, bound, and
annotation outputs.}

\item{use_cache}{Logical; if `TRUE`, reuse cached correlation results when
present.}

\item{cache_dir}{Optional cache directory for per-method correlation RDS
files.}

\item{verbose}{Logical; if `TRUE`, emit concise progress messages.}
}
\value{
The updated Module 1 multi-omic object with correlation outputs.
}
\description{
Run Pearson and Spearman correlations between condition-level TF expression
and quantile-normalized footprint scores, then keep TFBS calls using the
rebuilt Step 1 rule based on the maximum of the two correlation coefficients.
The default rebuilt output structure writes directly under
`predict_tf_binding_sites/` without a separate `all/` subfolder.
}
\examples{
\dontrun{
omics_data <- correlate_tf_to_fp(
  omics_data = omics_data,
  mode = "all",
  out_dir = file.path("predict_tf_binding_sites"),
  label_col = "strict_match_rna",
  db = "JASPAR2024",
  tf_subset = c("HNF1A", "SOX9"),
  use_cache = TRUE,
  verbose = TRUE
)
}
}
