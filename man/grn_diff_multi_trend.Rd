% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_grn_diff_multi.R
\name{grn_diff_multi_trend}
\alias{grn_diff_multi_trend}
\title{Multi-condition trend test for time/dose using limma}
\usage{
grn_diff_multi_trend(
  inputs,
  sample_info,
  covariate,
  add_covariates = NULL,
  value_col = "link_score",
  strict = TRUE,
  dedupe = TRUE,
  dedupe_method = c("max_link_score", "first"),
  verbose = TRUE
)
}
\arguments{
\item{inputs}{Character vector of CSVs or list of data.frames/tibbles.}

\item{sample_info}{Data.frame with one row per input, in same order.}

\item{covariate}{Name of numeric column in `sample_info` (e.g. `"time"`,
`"dose"`).}

\item{add_covariates}{Optional character vector of additional columns in
`sample_info` to include as fixed effects (e.g. `"batch"`, `"cell_line"`).}

\item{value_col}{Column in the link tables to model (default `"link_score"`).}

\item{strict, }{dedupe, dedupe_method Passed to `.load_multi_links_matrix()`.}

\item{verbose}{Logical.}
}
\value{
A tibble with:
  - `tf`, `gene_key`, `peak_id`.
  - `slope`, `se`, `t`, `p_value`, `fdr` for the covariate.
  - Optionally other design coefficients as `coef_<name>`.
}
\description{
Tests for a monotonic effect of a numeric covariate (time or dose) on
`value_col` per edge, using limma's moderated t-statistics. Returns effect
size (slope), SE, t, p, and FDR for the covariate.
}
