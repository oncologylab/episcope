% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_step3_topic_models.R
\name{extract_regulatory_topics}
\alias{extract_regulatory_topics}
\title{Extract regulatory topics from trained models (Module 3)}
\usage{
extract_regulatory_topics(
  k,
  model_dir,
  output_dir,
  topic_input_dir = NULL,
  backend = c("warplda", "vae"),
  vae_variant = "multivi_encoder",
  doc_mode = c("tf_cluster", "tf"),
  weight_label = c("peak_log2fc_fp_gene_fc_expr", "peak_delta_fp_gene_fc_expr",
    "peak_score_gene_expr"),
  flatten_single_output = FALSE,
  topic_report_args = list()
)
}
\arguments{
\item{k}{Integer K selected by the user.}

\item{model_dir}{Directory containing trained topic model outputs.}

\item{output_dir}{Directory to write final topic reports.}

\item{topic_input_dir}{Optional directory containing shared topic input
caches. When supplied, `dtm.rds` and `edges_docs.rds` can be loaded from
this directory if they are not present in the model directory.}

\item{backend}{Topic model backend. `"warplda"` is the default native
WarpLDA backend; `"vae"` reads optional VAE outputs.}

\item{vae_variant}{VAE variant name used in trained output directories.}

\item{doc_mode}{Document mode used during training.}

\item{weight_label}{Weight label used in trained output directories.}

\item{flatten_single_output}{If `TRUE` and only one trained output directory
is matched, write reports directly under `output_dir`.}

\item{topic_report_args}{Optional list of overrides for report settings.}
}
\description{
Uses precomputed topic models to compute link-topic scores, topic
assignments, and downstream reports for a user-selected K. The default
backend is native WarpLDA to match `train_topic_models()`.
}
