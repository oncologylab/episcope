% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_load_footprints_process.R
\name{load_atac}
\alias{load_atac}
\title{Build ATAC score/overlap tibbles; sort by chr/start/end first (if enabled).}
\usage{
load_atac(
  atac_data,
  chr_col = "Chr",
  start_col = "Start",
  end_col = "End",
  overlap_prefix = "Overlap_",
  metadata_cols = c("PeakID", "Distance.to.TSS", "Nearest.PromoterID", "Entrez.ID",
    "Nearest.Unigene", "Nearest.Refseq", "Nearest.Ensembl", "Gene.Name", "Gene.Alias",
    "Gene.Description", "Gene.Type"),
  sort_peaks = TRUE
)
}
\arguments{
\item{atac_data}{data.frame/tibble with coords, metadata, numeric samples, and overlap cols.}

\item{chr_col, start_col, end_col}{coordinate column names (defaults: "Chr","Start","End").}

\item{overlap_prefix}{prefix for overlap columns (default "Overlap_").}

\item{metadata_cols}{metadata columns to exclude from samples (besides coords).}

\item{sort_peaks}{logical: sort rows by chr/start/end before processing (default TRUE).}
}
\value{
list(score = atac_score, overlap = atac_overlap)
}
\description{
Build ATAC score/overlap tibbles; sort by chr/start/end first (if enabled).
}
