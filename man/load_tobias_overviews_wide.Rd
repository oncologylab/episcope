% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_fp_overview_loader.R
\name{load_tobias_overviews_wide}
\alias{load_tobias_overviews_wide}
\title{Load TOBIAS *_overview.txt across samples to a wide table (optionally limit samples)}
\usage{
load_tobias_overviews_wide(
  root_dir,
  db_name,
  sample_ids = NULL,
  n_samples = NULL,
  n_workers = max(1L, parallel::detectCores(logical = TRUE)),
  set_plan = TRUE,
  out_tsv = NULL,
  verbose = TRUE
)
}
\arguments{
\item{root_dir}{Character. Path with per-sample folders (e.g., "Y:/.../TOBIAS_merged_peaks").}

\item{db_name}{Character. Motif DB folder name inside each sample (e.g., "JASPAR2024", "HOCOMOCOv13").}

\item{sample_ids}{Optional character vector of sample folder names to include. Default: discover all.}

\item{n_samples}{Optional integer; if set, only the first `n_samples` (after filtering) are processed.}

\item{out_tsv}{Optional path to write the wide table as TSV. Default NULL (do not write).}

\item{verbose}{Logical; print progress messages. Default TRUE.}
}
\value{
A tibble with columns:
  - peak_ID (chr:start-end from TFBS_chr/start/end)
  - TFBS_name
  - <sample>_ATAC_score, <sample>_ATAC_bound for each processed sample
}
\description{
Load TOBIAS *_overview.txt across samples to a wide table (optionally limit samples)
}
\examples{
\dontrun{
wide <- load_tobias_overviews_wide(
  root_dir  = "Y:/cy232/cutntag/humanPDAC/Nutrients_basal/TOBIAS_merged_peaks",
  db_name   = "JASPAR2024",
  n_samples = 2,       # quick test on 2 samples
  verbose   = TRUE
)
}
Load TOBIAS *_overview.txt to a wide tibble (future-parallel) + keep peak_ATAC
}
