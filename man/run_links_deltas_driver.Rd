% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_step3_grn_diff.R
\name{run_links_deltas_driver}
\alias{run_links_deltas_driver}
\title{Run link-delta comparisons from a prebuilt specs table}
\usage{
run_links_deltas_driver(
  specs,
  clean_names = FALSE,
  parallel = TRUE,
  plan = "multisession",
  workers = NULL,
  dedupe = TRUE,
  pseudocount = 1,
  restrict_to_active_both = TRUE,
  edge_change_min = 0,
  keep_all_cols = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{specs}{Tibble/data.frame with columns:
`cond1_source`, `cond2_source`, `cond1_name`, `cond2_name`, `out_file`.}

\item{clean_names}{Logical; pass-through to `compare_links_bulk()`.}

\item{parallel}{Logical; pass-through to `compare_links_bulk()`.}

\item{plan}{Future plan name or function; pass-through to `compare_links_bulk()`.}

\item{workers}{Integer or NULL; pass-through to `compare_links_bulk()`.}

\item{dedupe}{Logical; pass-through to `compare_links_bulk()`.}

\item{pseudocount}{Numeric; pass-through to `compare_links_bulk()`.}

\item{restrict_to_active_both}{Logical; if TRUE, set delta/log2FC columns to NA
unless a link is active in both conditions (default TRUE).}

\item{edge_change_min}{Numeric; minimum |delta_link_score| used to classify
edges as gain/loss/shared (default 0).}

\item{keep_all_cols}{Logical; if TRUE, retain extra per-condition columns (default TRUE).}

\item{verbose}{Logical; pass-through to `compare_links_bulk()`.}
}
\value{
(invisible) list of result tibbles.
}
\description{
Run link-delta comparisons from a prebuilt specs table
}
