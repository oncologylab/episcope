% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_step1_direct_bound_filter.R
\name{write_direct_bound_filter_bed}
\alias{write_direct_bound_filter_bed}
\title{Write condition-specific direct-bound peak BED files from Module 1 outputs}
\usage{
write_direct_bound_filter_bed(
  step1_out_dir,
  conditions,
  db = "JASPAR2024",
  genome,
  r_cutoff = 0.3,
  out_dir = NULL,
  tf_subset = NULL,
  n_cores = max(1L, parallel::detectCores(logical = TRUE) - 1L),
  verbose = TRUE
)
}
\arguments{
\item{step1_out_dir}{Module 1 output directory, typically
`predict_tf_binding_sites/`.}

\item{conditions}{Character vector of condition names, for example
`c("AsPC1_10_FBS", "HPAFII_10_FBS")`.}

\item{db}{Motif database label used in the Step 1 outputs, default
`"JASPAR2024"`.}

\item{genome}{Reference genome used to select the packaged cluster map, for
example `"hg38"` or `"mm10"`.}

\item{r_cutoff}{Minimum absolute correlation required to keep a TFBS.}

\item{out_dir}{Output directory. Defaults to
`file.path(step1_out_dir, "direct_bound_filter")`.}

\item{tf_subset}{Optional TF subset.}

\item{n_cores}{Number of cores used for per-TF filtering.}

\item{verbose}{Logical; if `TRUE`, emit concise progress messages.}
}
\value{
A named character vector of written BED-file paths.
}
\description{
Apply a post-Step-1 direct-binding filter using Module 1 overview files,
footprint-bound condition flags, gene-expression flags, and the packaged
motif cluster map for the requested genome. For each requested condition,
this writes a BED file containing peaks that pass the correlation cutoff and
have at least one direct TF-binding assignment in that condition.
}
\examples{
\dontrun{
write_direct_bound_filter_bed(
  step1_out_dir = file.path("predict_tf_binding_sites"),
  conditions = c("AsPC1_10_FBS"),
  db = "JASPAR2024",
  genome = "hg38",
  r_cutoff = 0.30
)
}
}
\keyword{internal}
