% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_grn_link_network_topic.R
\name{grn_topic_validate_links}
\alias{grn_topic_validate_links}
\title{Validate and normalize a topic-centric GRN links table}
\usage{
grn_topic_validate_links(links, cols = list(), must_have_scores = character(0))
}
\arguments{
\item{links}{data.frame-like}

\item{cols}{optional name mapping list(tf, gene, enhancer, topic, condition)}

\item{must_have_scores}{character() of numeric score columns required}
}
\value{
standardized data.frame
}
\description{
Many \code{episcope} functions expect a "links" table in long format where each
row represents a TFenhancergene candidate relationship (optionally scoped to a
topic and a condition) with one or more scores (e.g., link score, footprint score).
}
\details{
Required columns:
\itemize{
  \item \strong{tf}: transcription factor symbol (character)
  \item \strong{gene}: gene symbol or ID (character)
  \item \strong{enhancer}: enhancer ID (character; e.g., "chr1:100-200")
  \item \strong{topic}: topic/group label (character or factor)
}
Optional columns:
\itemize{
  \item \strong{condition}: condition / group label
  \item numeric score columns (e.g., \strong{link_score}, \strong{fp_score})
}
}
