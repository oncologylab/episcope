% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_step3_topic_models.R
\name{build_doc_term_condition_union}
\alias{build_doc_term_condition_union}
\title{Build condition-level union document-term rows}
\usage{
build_doc_term_condition_union(
  edges_condition,
  count_method = c("bin", "log"),
  count_scale = 50,
  prefix_terms = TRUE,
  threshold_gene_expr = 0,
  threshold_fp_score = 0,
  threshold_tf_expr = -Inf,
  include_tf_terms = FALSE,
  require_tf_expr = FALSE,
  fp_term_mode = c("unique", "aggregate", "aggregate_weight"),
  balance_mode = c("min", "none"),
  check_repeated_values = TRUE
)
}
\arguments{
\item{edges_condition}{Condition-specific link table from
`add_condition_tf_docs()`.}

\item{count_method}{Count conversion method, either `"bin"` or `"log"`.}

\item{count_scale}{Numeric count scale.}

\item{prefix_terms}{Whether to prefix terms with `GENE:` or `PEAK:`.}

\item{threshold_gene_expr}{Minimum target-gene expression.}

\item{threshold_fp_score}{Minimum footprint score.}

\item{threshold_tf_expr}{Minimum TF expression.}

\item{include_tf_terms}{Whether to add the current TF as a gene-like
self-term in TF documents.}

\item{require_tf_expr}{Whether the document row requires TF expression.}

\item{fp_term_mode}{FP term mode: unique peaks, target-gene aggregated
peaks, or gene-only aggregate weights.}

\item{balance_mode}{Modality balancing mode, either `"min"` or `"none"`.}

\item{check_repeated_values}{Warn if repeated document-term rows have
inconsistent source values before deterministic collapse.}
}
\value{
A data.table with `doc_id`, `term_id`, `weight`, and count columns.
}
\description{
Builds condition-level document terms after `add_condition_tf_docs()`.
Target genes require `gene_expr_condition >= threshold_gene_expr`, FP terms
require `fp_score_condition >= threshold_fp_score`, and TF documents can be
filtered by `tf_expr_condition >= threshold_tf_expr`. The same document
construction logic is used for TF and TF-cluster condition documents.
}
\keyword{internal}
